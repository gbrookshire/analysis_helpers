A library of helper functions that might be useful for analyses of behavioral datasets in R. Install the most recent build in the builds/ directory like this:

install.packages('PATH/TO/FILE/analysisHelpers...tar.gz', repos=NULL, type='source')
